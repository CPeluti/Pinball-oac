CINUNI:.word 50,64,51,63,51,62,51,61,51,60,51,59,51,
64,52,63,52,62,52,61,52,60,52,59,52,
60,55,60,54,60,53,
59,55,59,54,59,53,
64,56,63,56,62,56,61,56,60,56,59,56,
64,57,63,57,62,57,61,57,60,57,59,57,
64,58,64,59,64,60,64,61,
63,58,63,59,63,60,63,61,
64,62,63,62,62,62,61,62,60,62,59,62,
64,63,63,63,62,63,61,63,60,63,59,63,
SEIUNI:.word 56,64,51,63,51,62,51,61,51,60,51,59,51,
64,52,63,52,62,52,61,52,60,52,59,52,
60,53,60,54,60,55,60,56,60,57,60,58,60,59,60,60,60,61,60,62,60,63,
59,53,59,54,59,55,59,56,59,57,59,58,59,59,59,60,59,61,59,62,59,63,
64,57,64,58,64,59,64,60,64,61,64,62,64,63,
63,57,63,58,63,59,63,60,63,61,63,62,63,63,
62,57,61,57,
62,58,61,58,
62,62,61,62,
62,63,61,63
SETUNI:.word 34,64,51,64,52,64,53,64,54,64,55,64,56,64,57,64,58,64,59,64,60,64,61,64,62,64,63,
63,51,63,52,63,53,63,54,63,55,63,56,63,57,63,58,63,59,63,60,63,61,63,62,63,63,
62,51,61,51,60,51,59,51,
62,52,61,52,60,52,59,52
OIUNI:.word 64,64,51,64,52,64,53,64,54,64,55,64,56,64,57,64,58,64,59,64,60,64,61,64,62,64,63,
63,51,63,52,63,53,63,54,63,55,63,56,63,57,63,58,63,59,63,60,63,61,63,62,63,63,
60,51,60,52,60,53,60,54,60,55,60,56,60,57,60,58,60,59,60,60,60,61,60,62,60,63,
59,51,59,52,59,53,59,54,59,55,59,56,59,57,59,58,59,59,59,60,59,61,59,62,59,63,
62,51,61,51,
62,52,61,52,
62,57,61,57,
62,58,61,58,
62,62,61,62,
62,63,61,63
NOVUNI:  .word 48,64,51,64,52,64,53,64,54,64,55,64,56,64,57,64,58,64,59,64,60,64,61,64,62,64,63,
63,51,63,52,63,53,63,54,63,55,63,56,63,57,63,58,63,59,63,60,63,61,63,62,63,63,
62,51,61,51,60,51,59,51,
62,52,61,52,60,52,59,52	
59,53,59,54,59,55,59,56,59,57,
60,53,60,54,60,55,60,56,60,57,
61,56,62,56,
61,57,62,57,
.text  
## começo da funcao SCORE_ADD ##
## recebe como argumento a pontuação (s0) ##	
score_add:
	addi sp,sp -12
	sw s0,0(sp)
	sw s2,4(sp)
	sw ra,8(sp)


	#li s0, 10200	#COMENTAR ESSA PARTE, JÁ QUE A FUNÇÃO JÁ RECEBE S0
	j ADD
	# fazer um sw na pontuação antes de modificá-la (use stack)
######## SCORE_ADD #######
# LIMPA O SCORE UNIDADE #
LUNI:	la t0, 	ATUALI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x252235		# cor branca
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LOOP3:	beq t3, t1, TERM3		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LOOP3	
TERM3:	ret
# LIMPA O SCORE DEZENA #
LDEZ:	la t0, 	ATUALI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x252235		# cor branca
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LOOP2:	beq t3, t1, TERM2		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-8
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LOOP2	
TERM2:	ret
# LIMPA O SCORE CENTENA #
LCENT:	la t0, 	ATUALI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x252235		# cor branca
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LOOP:	beq t3, t1, TERM		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LOOP	
TERM:	ret
# LIMPA O SCORE MILHAR #
LMIL:	la t0, 	ATUALI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x252235		# cor branca
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LOOP1:	beq t3, t1, TERM1		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LOOP1	
TERM1:	ret
# ADICIONA #
ADD:	li t0,100		# s0 = pontuação
	beq s0,t0,CE	
	li t0,200
	beq s0,t0,DU
	li t0,300		# s0 = pontuação
	beq s0,t0,TR
	li t0,400		# s0 = pontuação
	beq s0,t0,QA
	li t0,500
	beq s0,t0,QI
	li t0,600		# s0 = pontuação
	beq s0,t0,SE	
	li t0,700
	beq s0,t0,ST
	li t0,800		# s0 = pontuação
	beq s0,t0,OI	
	li t0,900
	beq s0,t0,NO
	j UNIMILHAR		# pula para a parte milhar, já que o número é maior que 900
CE:	jal LCENT
	la t0, 	UMUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007	# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP1:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP1	
DU:	jal LCENT
	la t0, 	DOUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007	# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP2:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP2
TR:	jal LCENT
	la t0, 	TREUNI	# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP3:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP3
QA:	jal LCENT
	la t0, 	QUAUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP4:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP4
QI:	jal LCENT
	la t0, 	CINUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP5:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP5
SE: 	jal LCENT
	la t0, 	SEIUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP6:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP6
ST:	jal LCENT
	la t0, 	SETUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP7:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP7
OI:	jal LCENT
	la t0, 	OIUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP8:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP8
NO:	jal LCENT
	la t0, 	NOVUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP9:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-16
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP9
UNIMILHAR:	li t0,2000
		blt s0,t0,CEM
		li t0,3000		# s0 = pontuação
		blt s0,t0,DUM
		li t0,4000		# s0 = pontuação
		blt s0,t0,TRM
		li t0,5000
		blt s0,t0,QAM
		li t0,6000		# s0 = pontuação
		blt s0,t0,QIM	
		li t0,7000
		blt s0,t0,SEM
		li t0,8000		# s0 = pontuação
		blt s0,t0,STM	
		li t0,9000
		blt s0,t0,OIM
		li t0, 10000
		blt s0,t0,NOM
		j F42D		# pula para a parte 42, já que o número é maior que 9000
CEM:	jal LMIL
	la t0, UMUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007	# cor 
	li s3, 1000
	sub s0,s0,s3		# subtrai o valor da pontuação 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP1M:	beq t3, t1, CHECKM		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP1M
DUM:	jal LMIL
	la t0, 	DOUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007	# cor 
	li s3, 2000
	sub s0,s0,s3		# subtrai o valor da pontuação 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP2M:	beq t3, t1, CHECKM		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP2M
TRM:	jal LMIL	
	la t0, 	TREUNI	# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li s3, 3000
	sub s0,s0,s3		# subtrai o valor da pontuação 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP3M:	beq t3, t1, CHECKM		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP3M
QAM:	jal LMIL
	la t0, 	QUAUNI	# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li s3, 4000
	sub s0,s0,s3		# subtrai o valor da pontuação  
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP4M:	beq t3, t1, CHECKM		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP4M
QIM:	jal LMIL
	la t0, 	CINUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li s3, 5000
	sub s0,s0,s3		# subtrai o valor da pontuação 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP5M:	beq t3, t1, CHECKM		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP5M
SEM: 	jal LMIL
	la t0, 	SEIUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li s3, 6000
	sub s0,s0,s3		# subtrai o valor da pontuação 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP6M:	beq t3, t1, CHECKM		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP6M
STM:	jal LMIL
	la t0, 	SETUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li s3, 7000
	sub s0,s0,s3		# subtrai o valor da pontuação 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP7M:	beq t3, t1, CHECKM	# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP7M
OIM:	jal LMIL
	la t0, 	OIUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li s3, 8000
	sub s0,s0,s3		# subtrai o valor da pontuação 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP8M:	beq t3, t1, CHECKM	# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP8M
NOM:	jal LMIL
	la t0, 	NOVUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007		# cor 
	li s3, 9000
	sub s0,s0,s3		# subtrai o valor da pontuação 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP9M:	beq t3, t1, CHECKM	# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-24
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP9M
CHECKM: beqz s0,F
	j ADD
F42D: jal LDEZ
	la t0, 	QUAUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007	# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP42D:	beq t3, t1, F42U		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	addi t5,t5,-8
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0, t0, 8		# soma 8 no endereço da Label CAMINHO
	j LP42D
F42U:	jal LUNI
	la t0, 	DOUNI		# carrega o endereço da Label CAMINHO em t0
	li s2, 0x00000007	# cor 
	li t3, 0		# contador
	li t4, 320		# num que deve ser multiplicado por Y
	lw t1, 0(t0)		# lê a quantidade de passos
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
LP42U:	beq t3, t1, F		# contador = número de passos termina o programa
	li t2, 0xFF000000 	# carrega o endereço do começo da mem. VGA (frame 1)
	lw t5, 4(t0)		# lê a coordenada X
	lw t6, 8(t0)		# lê a coordenada Y
	add t2, t2, t5		# soma endereço base + coord X
	mul t6, t6, t4		# multiplica Y*320
	add t2, t2, t6		# soma endereço base + coord X + coordY * 320
	sb s2, 0(t2)		# escreve 1 pixel de cor branca na frame 0 no end. da coordenada
	addi t3, t3, 1		# iteração
	addi t0,t0, 8
	j LP42U
F:	

	lw s2,4(sp)
	
	lw s0,0(sp)
	lw ra,8(sp)
	addi sp, sp, 12
	ret
